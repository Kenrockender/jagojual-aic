# Python
__pycache__/
*.pyc
.venv/
.env

# Node / Next
node_modules/
.next/
npm-debug.log*

# Data SFT = turunan deterministik dari data/dialogs/ via 2_prepare_sft.py.
# Tidak di-commit supaya sumber kebenaran tetap satu (dialog berlabel).
data/sft/

# Model — bobot besar TIDAK di-commit langsung; adapter LoRA (kecil) boleh.
# Yang di-commit: adapter, config, tokenizer, + training_meta.json & eval.json (bukti).
# Yang TIDAK: subfolder checkpoint-* (optimizer state, besar) tetap terabaikan.
model/checkpoints/*
!model/checkpoints/.gitkeep
!model/checkpoints/*.safetensors
!model/checkpoints/*.json
!model/checkpoints/*.jinja

# OS
.DS_Store
Thumbs.db
